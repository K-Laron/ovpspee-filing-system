# Implementation Plans

Generated by the improve skill on 2026-06-27. Execute in order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Security & correctness fixes (batch 1) | P1 | S | — | TODO |
| 002 | Unified verify script + README + doc fixes | P1 | S | — | TODO |
| 003 | Auth guards consolidation + preview/location dedup | P1 | S | — | TODO |
| 004 | Route-level code splitting | P2 | S | 002 | TODO |
| 005 | Status filter in SQL + scan intake polling fix | P2 | S | — | TODO |
| 006 | Paginated document listing | P2 | M | 005 | TODO |
| 007 | spawn_blocking for PDF export | P2 | M | — | TODO |
| 008 | Split commands.rs boilerplate registry | P2 | M | — | TODO |
| 009 | Split documents.rs god module | P2 | M | 008 | DONE (plan written) |
| 010 | Rust unit tests for auth module | P2 | M | 003 | DONE (plan written) |
| 011 | Frontend tests for core pages | P3 | L | 002 | DONE (plan written) |
| 012 | ESLint + formatter + CI infrastructure | P3 | M | 002 | DONE (plan written) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 004 (code splitting) depends on 002 because the verify script validates things work after the change.
- 006 (pagination) slightly easier after 005 (status filter) since both modify the same query path.
- 008 (split commands.rs) before 009 (split documents.rs) — same architectural pattern, do commands first as practice.
- 010 (Rust tests) depends on 003 (auth guards consolidation) — avoids writing tests for code that's about to change.
- 011 + 012 depend on 002 (verify script + README) so executors can validate their work easily.

## Findings considered and rejected

- SEC-02 (PowerShell injection via printer name): `ps_single_quote()` at `windows.rs:307` properly escapes for PowerShell single-quoted string context. By-design, not a finding.
- BUG-05 (expires_at string comparison): RFC 3339 `Z`-suffixed strings sort lexicographically identically to chrono comparison. Correct in current form.
- ARCH-17 (recently deleted files have zero remaining references): Already clean from commit 9b4b638. No work needed.
- DEP items: tower-http `trace` feature, base64 crate, chrono `serde` feature — all acceptable-cost dependencies. Not worth the churn to remove.
- PERF-17 (session churn on login): By-design decision per ADR-003 (single machine, one session per user). Changing would contradict documented intent.
